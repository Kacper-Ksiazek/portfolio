// Tools
import { styled } from "@mui/system";
// Styled components
export default styled("a")(({ theme }) => ({
    position: "absolute",
    top: "0",
    left: "0",
    height: "100%",
    width: "100%",
    zIndex: 10,
    cursor: "pointer",
}));
