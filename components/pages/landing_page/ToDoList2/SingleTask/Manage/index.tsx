// Tools
// Types
import type { FunctionComponent } from "react";
// Other components
import UnwindIconButton from "./UnwindIconButton";

interface ManageProps {
    //
}

const Manage: FunctionComponent<ManageProps> = (props) => {
    return <UnwindIconButton />;
};

export default Manage;
