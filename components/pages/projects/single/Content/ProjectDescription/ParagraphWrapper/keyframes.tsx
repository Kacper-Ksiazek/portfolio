// Tools
import { keyframes } from "@mui/material";

export const intro = keyframes({
    from: {
        width: 0,
    },
    to: {
        width: "100%",
    },
});
export const outro = keyframes({
    from: {
        width: "100%",
    },
    to: {
        width: 0,
    },
});
