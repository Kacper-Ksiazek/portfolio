generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Project {
  id           String   @id @default(cuid())
  title        String   @unique
  technologies String[]
  folder       String   @unique
  description  Json
  features     Json
}
